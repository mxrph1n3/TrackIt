export type GrowthPoint = {
  label: string;
  value: number;
  highlight?: boolean;
};

export type BarChartPoint = {
  label: string;
  value: number;
};

export type HabitHeatmapRow = {
  day: string;
  weeks: number[];
};
